# https://docs.travis-ci.com/user/reference/osx/

os: osx
osx_image: xcode11.3

install:
  - pwd
  - [ -d "dchima/dotfiles" ] && echo "Directory dchima/dotfiles exists."
  - [ -d "dotfiles" ] && echo "Directory dotfiles exists."
  - ls
  